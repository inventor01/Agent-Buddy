> [!NOTE]
> This repo started as a snapshot exported from a [Lovable](https://lovable.dev)
> project. It is a standalone git history now — pushes here do **not** sync
> back to Lovable, and Lovable pushes (if that project is still open) won't
> land here either. Treat the two as separate going forward.
